# Media to record before submission (on a machine with a browser)

1. `journey.gif` — the fix-it journey, 15–20s, looped, ≤ 8MB:
   ice-maker symptom → diagnosis card → "Check fits my model" (WDT780SAEM1) →
   verdict card → Add to cart → cart badge. (`make up` with MOCK_LLM=1 is fine.)
2. `block-product.png`, `block-compat.png`, `block-diagnosis.png`,
   `block-install.png` — one screenshot per rich block (crop tight).

Then update the image links at the top of the root README.
