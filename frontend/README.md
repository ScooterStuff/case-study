# PartSelect Chat — frontend

CRA app (adapted from the Instalily template — see `playbook/DEVIATIONS.md`).

```bash
npm install
npm start          # http://localhost:3000 (proxies /chat to localhost:8000)
```

`REACT_APP_API_BASE` overrides the backend URL (default: CRA dev proxy).
